variables:
  strip-binaries: ""
  tools: "/tools"
  prefix: "/tools"
  sysconfdir: "/tools/etc"
  localstatedir: "/tools/var"

environment:
  PATH: "%{tools}/bin:/bin"
  LD_LIBRARY_PATH: "%{tools}/lib:/lib"
