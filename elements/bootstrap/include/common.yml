variables:
  strip-binaries: ""
  tools: "/tools"
  prefix: "%{bootstrap-prefix}"
  sysconfdir: "%{bootstrap-prefix}/etc"
  localstatedir: "%{bootstrap-prefix}/var"

environment:
  PATH: "%{tools}/bin:/bin"
