variables:
  strip-binaries: ""
  tools: "/tools"
  prefix: "/usr"

environment:
  _GCC_AUTOCONF_VERSION_CHECK: false
  PATH: "%{tools}/bin:%{install-root}/usr/bin:/usr/bin"
  LD_LIBRARY_PATH: "%{tools}/lib:%{install-root}/usr/lib:/usr/lib"
